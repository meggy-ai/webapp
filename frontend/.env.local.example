# Bruno PA Frontend Environment Configuration
# Copy this file to .env.local and update the values for your local development

# ==============================================
# Backend API Configuration
# ==============================================

# The base URL for the Bruno PA backend API
# Development: http://localhost:8000/api
# Production: https://your-domain.com/api
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# API request timeout in milliseconds (default: 30000 = 30 seconds)
NEXT_PUBLIC_API_TIMEOUT=30000

# ==============================================
# WebSocket Configuration  
# ==============================================

# WebSocket URL for real-time features (chat, proactive messages, timers)
# Development: ws://localhost:8000/ws
# Production: wss://your-domain.com/ws
# Note: If not provided, will be auto-constructed from API URL
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# WebSocket reconnection interval in milliseconds (default: 5000 = 5 seconds)
NEXT_PUBLIC_WS_RECONNECT_INTERVAL=5000

# Maximum WebSocket reconnection attempts (default: 5)
NEXT_PUBLIC_WS_MAX_RECONNECT_ATTEMPTS=5

# ==============================================
# Application Configuration
# ==============================================

# Application display name
NEXT_PUBLIC_APP_NAME=Bruno Personal Assistant

# Application environment (development, production, test)
# Usually auto-detected from NODE_ENV, but can be overridden
NEXT_PUBLIC_APP_ENV=development

# ==============================================
# Next.js Configuration
# ==============================================

# Disable Next.js telemetry data collection (optional)
NEXT_TELEMETRY_DISABLED=1

# ==============================================
# Example Production Configuration
# ==============================================

# For production deployment, update the URLs:
# NEXT_PUBLIC_API_URL=https://api.brunopa.com/api
# NEXT_PUBLIC_WS_URL=wss://api.brunopa.com/ws
# NEXT_PUBLIC_APP_ENV=production

# ==============================================
# Development Notes
# ==============================================

# 1. This file should be copied to .env.local for local development
# 2. .env.local is gitignored and contains your personal configuration
# 3. All NEXT_PUBLIC_ variables are exposed to the browser
# 4. Never put sensitive data in NEXT_PUBLIC_ variables
# 5. Restart the development server after changing environment variables
# 6. Default values are defined in lib/config.defaults.ts
# 7. Environment variables override defaults from config files
# 8. Production requires NEXT_PUBLIC_API_URL and NEXT_PUBLIC_WS_URL to be set